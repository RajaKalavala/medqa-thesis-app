Decompose the following medical board-style question into at most {{ n }}
focused sub-queries that, taken together, would let you answer it.
Each sub-query must target one fact, mechanism, or differential.
Return only the sub-queries, one per line, with no numbering or commentary.

{{ question_block }}
